# Suggested starting point for working the challenges.
# Not a constraint - add whatever your approach needs.

pandas>=2.0,<3.0
numpy>=1.24
scikit-learn>=1.3
matplotlib>=3.7
langdetect==1.0.9          # challenge 1 (language filtering)
scipy>=1.11                # challenge 2 (optimal cluster-label assignment)

# Challenges 9 and 10 need a deep learning framework. Both are large, so install
# only the one you need rather than uncommenting both.
# torch>=2.2               # challenge 9
# tensorflow>=2.16          # challenge 10 (provides Keras)

# Optional, for exploratory work:
# jupyterlab>=4.0
